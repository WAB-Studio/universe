# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# eslint cache
.eslintcache

# typescript
*.tsbuildinfo
next-env.d.ts

# local-only agent instructions (machine-specific, never committed)
CLAUDE.local.md

# local-only agents (machine-specific setup, never committed)
.claude/agents/integrator.md
.claude/skills/orchestrator-local/

# plans and local working notes: readable, never committed
private/
# OpenAI usage exports: account-wide, not this app's, and never a commit.
completions_usage_*.csv
.claude/usage-log.tsv

# the dictionary build's downloaded tarball and extracted TEI source
apps/voyager/.cache/

# The reader's own production screenshots, kept out of the repo.
SS app/

# Recibos reales para la prueba de lectura (§4e). Llevan nombre, NIT, teléfono,
# correo y números de cuenta. No se commitean.
testmodelsphoto/
